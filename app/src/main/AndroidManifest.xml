<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.inventario">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Inventario">
        <activity android:name=".Launcher"
            android:label="@string/app_name"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Log"/>
        <activity android:name=".MainActivity" />
        <activity android:name=".AlternativeLog"/>
        <activity android:name=".AlternativeMainActivity" />
        <activity android:name=".AlternativeVerUbicacion" />
        <activity android:name=".VerUbicacion" />
        <uses-library android:name="com.symbol.emdk" android:required="false"/>
    </application>
    <uses-permission android:name="com.symbol.emdk.permission.EMDK" />

</manifest>